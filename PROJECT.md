# auto-band-selector

## 1.1.5 — KT Hidden Menu 경로

* 제공된 화면의 KT Hidden Menu → Network Setting 경로를 추가했습니다.
* com.sec.android.app.hiddenmenu 패키지 프로필을 추가하고 실제 인식 패키지·화면을 로그에 남깁니다. 기기의 패키지명은 실기기 로그로 확인해야 합니다.
* 앱 PiP가 포커스를 가진 경우 대상 패키지의 유일한 인터랙티브 창을 찾아 처리합니다.

## 1.1.4 — 히든 메뉴 마지막 숫자 입력

* 전화 앱에는 마지막 숫자를 제외한 번호를 전달하고 접근성으로 입력값을 확인한 뒤 마지막 8을 누릅니다. 기존 전체 번호가 남아 있어도 다시 입력합니다.
* 영어 Phone 제목이 없는 다이얼 화면은 허용된 패키지의 유일한 입력란과 0–9 키패드 구조로 인식합니다. 실제 기기 호환성은 추가 확인이 필요합니다.

## 1.1.3 — 전화 앱 전환 안정화
* 삼성 전화 앱을 여는 동안 앱·런처·System UI에서 발생하는 접근성 이벤트를 실행 손실로 오인하지 않도록 수정했습니다.
* 실제 동작은 계속 허용 목록의 삼성 전화 앱 루트에서만 수행하며, 기기 잠금·접근성 서비스 중단·단계별 시간 초과 안전장치는 유지합니다.

## 1.1.2 — 스캔 시작 및 로그 개선
* Android 논리 SIM 인덱스(0/1)를 삼성 메뉴 슬롯(1/2)로 변환하고 삼성 전화 앱 패키지 조회 선언을 추가했습니다.
* 전화 앱 실행을 메인 스레드로 옮기고, 시작 버튼의 접근성 이벤트가 실행을 취소하지 않도록 수정했습니다. 실행 중 상태도 시작 요청과 함께 즉시 표시합니다.
* 진행 단계, 전화 앱 실행, 접근성 결과, 실패·중단·복구 결과를 누적 로그에 기록합니다. 알려진 진입 코드와 비밀번호는 로그에서 가립니다.
* 로그 팝업에 Liquid Glass 디자인, 텍스트 선택, 전체 복사, 초기화, 공유, 닫기를 제공합니다. 초기화 후 팝업 내용도 즉시 갱신됩니다.
* 실제 삼성 펌웨어의 메뉴 호환성과 전체 스캔 완료 여부는 기기에서 확인해야 합니다. 실패 시 전체 로그를 복사해 원인을 확인할 수 있습니다.

## 1.1.1 — 시작 화면 렌더링 수정
* Liquid Glass 배경 캡처를 그라데이션 전용 레이어로 분리했습니다. 카드와 버튼이 자신을 포함한 배경을 다시 그리는 순환 참조를 제거했습니다.
* `StartupRenderingTest`는 메인 화면을 실제로 캡처하고 Activity 재생성 후 다시 확인합니다. GPU 렌더링 검증을 위해 하드웨어 가속 Android 기기에서 실행해야 합니다.
* 루트 `auto-band-selector.apk`는 설치 가능한 debug 서명 빌드입니다. 실기기 크래시 해결 여부는 기기에서 추가 확인이 필요합니다.

렌더링 회귀 테스트(기기 연결 후):
```sh
./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.sleepysoong.autobandselector.qa.StartupRenderingTest
```

## 프로젝트 개요
*   **제작자:** sleepysoong (hyunnn1123@naver.com)
*   **대상 기기:** KT 출하 **Galaxy Z Fold6(KT eSIM)**. 시작 버튼을 누를 때마다 KT LTE B1/B3/B8을 새로 측정해 가장 빠른 대역을 검증 후 적용합니다.
*   **작동 방식:** 접근성 서비스가 삼성 전화 앱의 히든 메뉴(`319712358` 진입 + 비밀번호 `774632`)를 상태 확인 상태로만 탐색합니다. 클릭 성공 여부가 아니라 신선한 화면 상태와 서비스 모드(`*123456#`) 등록 대역을 기준으로 성공을 판정합니다.
*   **최종 빌드본:** 레포지토리 루트의 `auto-band-selector.apk`가 소스와 동일한 빌드입니다. SHA-256과 서명은 릴리스 기록에서 확인할 수 있습니다.

## 사용 방법
1. 앱 설치 후 실행합니다. 재부팅만으로는 어떤 자동 실행도 일어나지 않습니다.
2. **접근성 서비스 활성화** 버튼과 READ_PHONE_STATE 권한을 먼저 허용합니다. KT eSIM이 기본 데이터 SIM이 아니면 시작이 차단됩니다 — SIM 설정을 열어 eSIM을 기본 데이터로 지정하세요.
3. **자동 주파수 스캔 시작**을 누릅니다. 매번 B1→B3→B8 순서로 대역을 임시 적용하고 각각 3회씩 3MB(총 최대 27MB, 통신 오버헤드 제외) 셀룰러 다운로드를 측정합니다. 중앙값이 가장 큰 대역을 최종 적용하고 다시 확인합니다.
4. 측정 실패·메뉴 부재·SIM 불일치는 어느 대역도 승자로 표시하지 않습니다. 실패 시 자동(Automatic) 복구를 먼저 확인하고, 복구도 실패하면 명시적 오류로 끝납니다.
5. **일시 중지**는 실행을 즉시 중단하며 복구는 하지 않습니다. **자동 모드 복구**는 별도 버튼으로 SELECTION 해제와 Automatic 복귀를 다시 확인합니다.
6. 결과는 로컬 로그(`logs.txt`, 1 MiB 순환)와 최근 20회 히스토리에만 기록됩니다. ICCID/IMSI 등 식별자, 비밀번호는 기록하지 않습니다.
7. 앱은 PiP 모드에서 현재 단계와 대역만 표시하고, PiP의 **중지** 액션으로 언제든 멈출 수 있습니다.

## 한계
*   대역 설정은 영구적 변경을 약속하지 않습니다 — 새 시작은 항상 새로 측정합니다.
*   삼성 펌웨어가 히든 메뉴 구조를 바꾸면 앱은 클릭 대신 지원 불가로 실패합니다.
*   LTE B1/B3/B8 단일 후보만 비교합니다. 5G·다중 조합 최적화는 제공하지 않습니다.
*   VPN이 활성이면 직접 셀룰러 비교를 진행하지 않습니다(우회하지 않습니다).

## 개발 환경
*   Kotlin 2.4.10, AGP 9.3.2, Gradle 9.7.1, compileSdk 37, minSdk 26, targetSdk 34
*   Compose(멀티플랫폼 아티팩트 1.12.0) + Kyant0 Backdrop 2.0.1 실제 글래스 머티리얼
*   검증: JVM/Robolectric 114 테스트, lint 오류 0, 디버그 APK·안드로이드테스트 APK 조립. 실기기 라디오 시나리오는 사용자 테스트로 예정돼 있으며 문서에 미검증으로 명시.
